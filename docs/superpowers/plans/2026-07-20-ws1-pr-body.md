# PR: WS1 — fix b2_library memorization (cardinality caps + TEMPORAL jitter) and lazy vLLM ignition

Base: `master` ← `ws1-b2-memorization-fix`
Create at: https://github.com/albertols/synthetic-llm-dataflow-bigquery/pull/new/ws1-b2-memorization-fix

## Summary

Remediates the 2026-07-20 b2 E2E run defects (spec `docs/superpowers/specs/2026-07-20-e2e-remediation-rag-eval-evolution-design.md` §3, WS1):

- **§3a — memorization fix**: `b2_library`'s classifier gains cardinality caps (20, mirroring B.1). Above the cap, DATE/TIME/TIMESTAMP columns and date-shaped STRINGs become the new `ColumnKind.TEMPORAL` — sampled as novel values uniformly within the observed `[min, max]` (epoch-based, per-value-type rendering) instead of verbatim empirical resampling (11 columns landed at `copy_ratio=1.0`). Non-temporal high-cardinality STRINGs reroute to the LLM `FreeTextHook`. CTGAN never sees temporal columns.
- **§3b — lazy vLLM ignition**: `VLLMModelClient.generate_json()` now calls its own idempotent, lock-serialized `setup()` on first use; the DoFn no longer ignites eagerly. Runs whose columns never reach the LLM skip the ~230 s / 519 GPU-s vLLM bring-up. Loud-failure semantics preserved (strict_freetext; boot errors raise out of the first LLM call).
- **§3c — observability**: `column_nonprintable` WARNING milestone when ≥5% of a STRING/BYTES column's values carry C0/C1 control characters (the COL_048 BYTES-as-STRING signature).
- **style**: repo-wide ruff 0.15.13 cleanup (40 pre-existing errors → clean), separate commit.

## Test plan

- [x] `uv run --no-sync python3 -m pytest packages/sdfb-tests -m "not gpu and not gcp" -q` — 480 passed
- [x] `uv run --no-sync ruff check .` — clean
- [ ] M4 follow-up: re-run the b2 tier per `docs/RUN_PLAYBOOK.md`; acceptance = `memorization_flags=[]`, no `model_client_setup_start` milestone on LLM-free runs, temporal `copy_ratio` ≤ b1 2026-07-19 levels (0.007–0.156)

🤖 Generated with [Claude Code](https://claude.com/claude-code)

# Commits to skip in `git blame` (`git config blame.ignoreRevsFile .git-blame-ignore-revs`).
# f4e61fa (#21): whitespace-only yapf reformat to Google style (2-space indent),
# squashed together with pylint-driven fixes and pragmas.
f4e61faeae445ff89bee9506e063d0051a4a0fcb
